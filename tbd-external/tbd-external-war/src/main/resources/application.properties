# Application properties
# These can be overridden by environment variables

# Database configuration
# These are fallbacks - environment variables take precedence
db.url=${DB_URL:jdbc:oracle:thin:@localhost:1521:ORCL}
db.username=${DB_USERNAME:tbd_user}
db.password=${DB_PASSWORD:change_me}
db.pool.size=${DB_POOL_SIZE:10}

# Application settings
app.name=TBD Internal
app.version=2.0.0-SNAPSHOT
